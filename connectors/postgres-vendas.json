{
  "name": "postgres-vendas",
  "class": "com.leonardoramos.rootl_cdcpublisher.adapters.inbound.postgres.PostgresReplicationAdapter",
  "config": {
    "jdbcUrl": "jdbc:postgresql://localhost:5432/vendas?replication=database",
    "user": "cdc_user",
    "password": "cdc_password",
    "slotName": "cdc_slot_vendas",
    "database": "vendas"
  }
}
